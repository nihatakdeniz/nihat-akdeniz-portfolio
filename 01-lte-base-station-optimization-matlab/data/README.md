# LTE map data note

The MATLAB scripts in `src/` expect a local OpenStreetMap building file named `map (3).osm`.

The raw OSM extract was found locally but is not included in this upload candidate. Add the file only after checking the source attribution and licensing requirements. The scripts use the Kırıkkale/Yahşihan campus area and should be reviewed before public publication.
